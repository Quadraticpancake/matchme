/*
require('./dbHelpers')
import {expect} from chai
import {addMessage} from '../db/chatHelpers'

describe('db helper functions for the chats', () => {
	describe('addMessage', () => {
		it('should add a new message', () => {

		});
	});
});
*/