var assert = require('assert');
var random_name = require('../');

var valid_name = function(n) {
  assert(/^[A-Z][a-z]*$/.test(n));
};
var valid_fullname = function(n) {
  assert(/^[A-Z][a-z]* [A-Z][a-z]*$/.test(n));
};
